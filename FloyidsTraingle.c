#include<stdio.h>
int main(){
int rows,i,j,number =1;printf("Enter the number rows: ")
scanf("%d ",&rows);
for (j = 1; j <= i; ++j) {
 printf("%d ", number);
 ++number;
 }
 printf("\n");
 }
 return 0;
}

}
